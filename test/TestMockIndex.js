// 'use strict';

// const MockIndex = artifacts.require("MockIndex");
// const Promise = require('bluebird');

// contract('MockIndex', (accounts) => {

//     it("MockIndex should be able to deploy.", async () => {
//         return await Promise.all([
//           MockIndex.deployed(),
//         ]).spread((/*MockIndex*/ core) => {
//             assert.ok(core, 'MockIndex contract is not deployed.');
//           });
//       });
    
//     // it("MockIndex should be able to return false.", async () => {
//     //     let instance = await RiskControl.deployed();
//     //     let result = await instance.hasRisk(accounts[0], accounts[1], "0x00eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee", web3.toWei(1), 10 ** 18);
//     //     assert.equal(result, false);
//     // })
// });
